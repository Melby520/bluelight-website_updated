# bluelight-website_updated
BLUELIGHT_update

## Clone the repository on your desktop

```bash
git clone https://github.com/Melby520/bluelight-website_updated.git
cd bluelight-website_updated
```

## Sync your local copy with the latest `master`

If you already cloned earlier and only fetched before, run:

```bash
git fetch origin
git checkout master
git pull --ff-only origin master
```
